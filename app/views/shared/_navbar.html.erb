<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'rDiplomacy', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to 'Games', root_path, class: 'nav-link' %>
      </li>
    </ul>
    <div class="form-inline my-2 my-lg-0">
      <% if user_signed_in? %>
        <div class="avatar avatar-xl">
          <img class="rounded-circle" src="<%= current_user.gravatar_url %>" alt="" width="30" height="30" />
        </div>
        &nbsp;
        <%= button_to 'Logout', users_sign_out_path, method: :delete, class: 'btn btn-sm btn-secondary' %>
      <% else %>
        <%= button_to 'Login with Google', user_google_oauth2_omniauth_authorize_path, method: :post, data: { turbo: 'false' }, class: 'btn btn-sm btn-primary' %>
      <% end %>
    </div>
  </div>
</nav>
